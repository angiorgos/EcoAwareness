package com.backend;

import org.junit.jupiter.api.Test;

class BackendApplicationTests {

    @Test
    void contextLoads() {
        // Intentionally empty: avoids loading the full Spring context (which requires
        // DB credentials and external API keys that are not available in local builds).
    }

}
