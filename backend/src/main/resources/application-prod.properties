# Production-only overrides.
# Activate by setting SPRING_PROFILES_ACTIVE=prod (e.g. in the docker container env).

# The reverse proxy terminates TLS, so the cookie must be marked Secure
# (browser only sends it over HTTPS).
server.servlet.session.cookie.secure=true